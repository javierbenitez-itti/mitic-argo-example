image:
  repository: harbor.homelab.arpa/library/mi-java-app
  tag: "1.0.0-SNAPSHOT"
  pullPolicy: Always

replicaCount: 1

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  className: nginx
  host: mi-app-dev.tu-dominio.com

env:
  APP_ENV: dev
